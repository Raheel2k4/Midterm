// Using the IP address from your saved preferences.
// This is necessary because 'localhost' won't work from the mobile app/emulator.
// Your backend server must be running on the same network.
const API_URL = 'http://10.0.4.118:3000';

export default API_URL;